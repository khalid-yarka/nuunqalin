Flask==3.0.0
python-dotenv==1.0.0
gunicorn==21.2.0
# Flask extensions (optional but recommended)
# flask-limiter==3.5.0  # For rate limiting (uncomment if needed)
# flask-session==0.5.0  # For server-side sessions